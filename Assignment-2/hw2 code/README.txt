Instructions to run the code:

	1. Include all the dataset files in the same directory as the 
	   code files

	2. The code file is a script written in Python (.py file). It requires a Python interpreter to run.

		PACKAGES USED:

			numpy 1.13.3
			matplotlib 2.2.2
			pandas 0.21.0
